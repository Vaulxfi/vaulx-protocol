# Vaulx — Changelog

Chronological log of build progress. Newest at the top.

---

## 2026-04-24 — Phase 3 kickoff (tasks 3.0–3.5)

- **Task 3.5 completed (lender auctions + indexer extension):** Indexer (`apps/indexer/src/main.ts`) gains a third `Subscription` for the auction program — `auctionCreated`/`bidPlaced`/`auctionClosed`/`auctionClosedNoBids` (lowercased per Anchor 0.30.1) land in `public.onchain_events` with the auction program id. Two new Next.js App Router API routes: `GET /api/auctions` dedupes `auctionCreated` rows from Supabase, fetches live on-chain state via Anchor's `Program.account.auction.fetch`, computes derived `OPEN/ENDED/CLOSED` status from `{status, end_ts}`, returns `{auctions, source}`; `GET /api/auctions/[id]/bids` returns latest 20 `bidPlaced` rows filtered defensively against both `payload->>auction` and `payload->>auctionPda`, sorted slot desc. Both routes `runtime="nodejs"` + `dynamic="force-dynamic"` with graceful `supabase_not_configured` fallback. **`lib/chain/auction.ts`** exports `deriveAuctionPda`, `deriveAuctionAuthorityPda`, `deriveStatus`, `shortPda`, and hooks `useAuctionList` (15s refetch), `useAuction` (10s refetch via on-chain fetch), `useAuctionBids` (10s refetch), `usePlaceBid` (derives escrow + bidder + previous-bidder ATAs per IDL; passes bidder's own ATA for `previous_bidder_ata` when `high_bidder == default`), `useCloseAuction` (full account list incl. `auction_authority` + `instructions_sysvar` + trdc/vault program IDs + token program). **`<AuctionCard>`** component with 1s client-side countdown and gold/ruby/grey status pill. **`/lend/auctions`** — editorial "The Foreclosure Floor" intro + 2 stat tiles (open count + reserve notional) + operator-grade hairline table with hover gold left-rule + empty state. **`/lend/auctions/[id]`** — 7/5 editorial split: left panel shows status chip + reserve/high-bid/min-increment + countdown + bid history table ("Be the first to bid" empty state) + permissionless "Close auction" card visible when ENDED; right panel has RHF+Zod bid form with min-bid computation (`reserve` or `high_bid + min_increment`), balance guard, OPEN-only enabled, toast + invalidation on success. **`/lend`** sidebar gains a "Foreclosure floor" card linking to `/lend/auctions` (SiteHeader top-level nav unchanged). Escrow ATA derivation uses `allowOwnerOffCurve=true` since auction PDA is off-curve. `BN` ↔ `bigint` conversion via `new BN(x.toString())`. Web + indexer typecheck + build green; `pnpm -w test` green. Commit `669dd86`.

## 2026-04-24 — Phase 3 kickoff (tasks 3.0–3.4)

- **Task 3.4 completed (I3 Solana Pay QR):** `GET/POST /api/solana-pay/[kind]/[trdc]` transaction-request endpoint where `kind ∈ {pay, repay, renew}`. GET returns `{label: "Vaulx", icon: data-URL SVG}`. POST accepts `{account}` + optional query params (`amount` for pay; `termDays`/`newDueTs`/`newRateBps` for renew), validates PDA + USDC mint + fetches on-chain account to 404 early on missing TRDCs, builds the right ix via the shared `buildLoanIxAccounts()` helper (extracted from `lib/chain/loan.ts` into new `lib/chain/loan-accounts.ts`), appends a `VAULX <KIND> <trdc-prefix>` memo ix, sets `feePayer = account`, serialises legacy tx without requiring signatures, returns `{transaction: base64, message}`. Permissive CORS + `OPTIONS` handler; `runtime = "nodejs"`, `dynamic = "force-dynamic"`. New `<SolanaPayQr>` component renders scannable `solana:<https-url>` deep-link via `qrcode.react` with disabled/grey state when form inputs aren't valid yet. Integrated into pay/repay/renew pages as stacked panels alongside desktop wallet buttons — pay QR enables when amount valid; repay always active; renew reflects selected term. `.env.example` documents `NEXT_PUBLIC_APP_URL` + HTTPS-for-mobile caveat. Deps added: `@solana/pay ^0.2.6`, `bignumber.js ^11.0.0`, `qrcode.react ^4.2.0`. Web build + typecheck green; GET/POST smoke-tested against `next dev`. Reference-pubkey injection + `/api/onchain-events/loan-payment` polling route deferred per spec. Commit `2a07d2a`.

## 2026-04-24 — Phase 3 kickoff (tasks 3.0–3.3)

- **Task 3.3 completed (borrower loan dashboard — Moments 5+6):** Repurposed `/borrow/loans/[trdc]/disburse` from Task-2.9 placeholder to the **loan dashboard** (hero status badge, 3 big mono stats — principal remaining / payoff today / due+days remaining, 3 editorial CTA cards with status-aware gating). Three new action pages `/borrow/loans/[trdc]/{pay,renew,repay}` all editorial 5/7 split with shared `<LoanContextPanel>` (status chip + appraisal + principal + rate + due + accrued + payoff + doc-hash). **Pay page:** RHF+Zod amount form, running totals, overpay guard, stays on page for repeat payments. **Renew page:** 30/60/90 radio, client-derives `new_rate_bps` + `new_due_ts`, breakdown (accrued interest + 2% flat fee → total), redirects to `/pay` on success. **Repay page:** hairline breakdown table with gold total row, warning panel, redirects to `/repaid` (new editorial thank-you placeholder). New `useLoanSummary(trdc)` hook (TanStack 10s refetch, BN→bigint normalisation, decoded status enum, derives `daysElapsed`/`daysRemaining`/`accruedAtoms`/`payoffAtoms`/`renewalFeeAtoms`/`isOverdue`/`isTerminal` via `@vaulx/terms`). Three new mutation hooks `useLoanInstallment`/`useLoanRenew`/`useLoanRepay` wired to the post-3.1 account lists (trdc_state, vault, asset_mint, vault_ata, borrower_ata, borrower, loan_authority, trdc_program, vault_program, token_program, instructions_sysvar). `useCreateCcbTrdc` updated — now accepts `termDays` and derives `rate_bps` via `rateForTermDays`, fixing the Task 3.1 FE staleness. Terms wizard pipes `termDays` through. CTA gating: active/renewed/overdue enabled (overdue within grace still payable); pendingCustody/activeInCustody/repaid/defaulted/liquidated disabled with per-status hints. Accrued-interest math uses `principalRemainingAtoms` to mirror the Rust implementation's post-installment behavior. `vault_ata` derivation uses `allowOwnerOffCurve=true` matching the `useDeposit` pattern. Web build + typecheck + 11/11 vitest green. Commit `1d6fa25`.

## 2026-04-24 — Phase 3 kickoff (tasks 3.0–3.2)

- **Task 3.2 completed (auction program + `execute_af_default`):** New `Auction` PDA at `[b"auction", trdc_state.as_ref()]` with `{trdc_state, asset_mint, reserve_price, min_increment, start_ts, end_ts, high_bid, high_bidder, escrow_ata, vault, status, bump, _reserved[32]}`, `AuctionStatus {Open, Closed}`. Three instructions: **`create_auction(reserve_price, duration_secs)`** — CPI-only from loan program with identical 2-layer gate to `vault.disburse` (loan_authority PDA signer + instructions-sysvar top-level-program check); initializes the auction + escrow ATA owned by the auction PDA; `min_increment = max(1, reserve_price / 100)`. **`place_bid(amount)`** — permissionless; enforces `amount >= reserve_price` and `amount >= high_bid + min_increment` (first bid bypasses increment); refunds prior high bidder from escrow via PDA-signed transfer; updates high/high_bidder atomically. **`close_auction()`** — permissionless after `now >= end_ts`; no-bid path flips TRDC `Defaulted → Liquidated` + emits `AuctionClosedNoBids`; bid path transfers escrow → vault_ata, CPIs `vault::record_auction_inflow` to credit `total_assets`, flips TRDC, emits `AuctionClosed`. **`vault::record_auction_inflow`** new with its own 2-layer gate (hardcoded `AUCTION_PROGRAM_ID` const + `[b"auction_authority"]` PDA signer) — kept separate from `record_inflow` so the loan-only gate on that path stays auditable. **`loan.execute_af_default(duration_secs)`** permissionless with `GRACE_PERIOD_SECS = 3 days`; validates `now > due_ts + grace` (else `NotYetDefaulted`); CPIs `trdc::transition_active_to_overdue` + `trdc::transition_overdue_to_defaulted`; computes `reserve_price = compute_payoff(principal_remaining, rate_bps, created_at, now)` via shared `math.rs`; CPIs `auction::create_auction` with `invoke_signed` using `[b"loan_authority"]` seeds; emits `AfDefaultExecuted`. Three new trdc transition ixs (`transition_active_to_overdue`, `transition_overdue_to_defaulted`, `transition_defaulted_to_liquidated`). 6 named tests in `tests/auction.spec.ts`: `test_default_kicks_off_auction`, `test_bid_monotonic` (refund + increment enforcement), `test_close_auction_liquidates_trdc`, `test_winning_bid_recovers_vault_capital`, `test_auction_close_no_bids_still_liquidates`, `test_not_yet_defaulted_reverts`. **45/45 anchor tests green** (39 baseline + 6 new). Tests use real wall-clock sleeps to pass `end_ts` (duration 4s); suite runtime ~4m. IDLs copied. Commit `9f46172`.

## 2026-04-24 — Phase 3 kickoff (tasks 3.0–3.1)

- **Task 3.1 completed (repayment + renewal lifecycle + interest math):** `@vaulx/terms` gains three new pure-TS modules — `rate.ts` (`RATE_TABLE_BPS: {30:800, 60:1000, 90:1200}`, `rateForTermDays`), `interest.ts` (BigInt `computeInterestAccrued` + `computePayoff` with linear simple-interest `floor(principal * rateBps * days / (10_000 * 365))`), `renewal.ts` (2% flat `RENEWAL_FEE_BPS` + total-to-renew helper). 16/16 terms vitest green (4 prior LTV + 12 new). **TRDCState** extends with `principal_remaining: u64` + `rate_bps: u64`; `_reserved` shrunk 32→16 bytes, account `SIZE` unchanged (no migration). `create_ccb_trdc` takes new `rate_bps: u64` param. Three new loan ixs: `pay_installment(amount)` (Active-state only; reverts `OverPayment` if amount > principal_remaining), `repay_ccb()` (no amount — computes payoff on-chain via `programs/loan/src/math.rs` which mirrors TS formulas u128-exact; transitions TRDC `Active→Repaid`), `renew_ccb(new_term_days, new_due_ts, new_rate_bps)` (charges accrued + flat fee; transitions `Active→Renewed→Active` in one step; resets `created_at` clock). Three new events `installmentPaid`/`ccbRepaid`/`ccbRenewed`. **Architectural fix:** TRDCState is owned by the trdc program so outer-frame mutations from loan are silently dropped — all state writes moved into dedicated trdc CPI ixs (`apply_installment`, `transition_active_to_repaid`, `transition_renew(new_due_ts, new_rate_bps)`). Similarly `Vault.total_assets` credits on repayment go through a new `vault::record_inflow` ix with the same 2-layer (`loan_authority` PDA + top-level-program-id) gate as `disburse` — only the loan program can bump vault assets outside `deposit`. `programs/loan/src/math.rs` unit-tested via `cargo test -p loan --lib` against the same golden inputs as the TS tests. Named tests in `tests/repayment.spec.ts`: `test_pay_installment_reduces_principal`, `test_repay_ccb_transitions_to_repaid`, `test_renew_ccb_extends_due_ts`, `test_payoff_math_matches_terms_package`. **39/39 anchor tests green** (35 baseline + 4 new). `pnpm -w typecheck` + `pnpm -w test` green. FE `apps/web/src/lib/chain/loan.ts` intentionally left stale — Task 3.3 updates the wizard's `create_ccb_trdc` call to pass the new `rate_bps`. Commit `41fbf9c`.

## 2026-04-24 — Phase 3 kickoff (task 3.0)

- **Task 3.0 completed (Civic Pass operational close-out):** All 6 `TODO(civic-sdk-verify)` markers resolved. **Critical correctness fix:** the Phase-2 Borsh parser in `programs/{vault,loan}/src/civic.rs` was skipping the `owner_identity: Option<Pubkey>` field that sits between `owner` and `gatekeeper_network` — it would have rejected every real Civic token. Parser rewritten to the exact `@identity.com/solana-gateway-ts` `GatewayTokenData` schema: `version[u8] + Option<Pubkey> parent + Pubkey owner + Option<Pubkey> owner_identity + Pubkey gatekeeper_network + Pubkey issuing_gatekeeper + u8 state + Option<u64> expiry` + optional expiry freshness check. Canonical Civic gateway program id also corrected from the padded `...UJU9` typo to the real SDK-exported `gatem74V238djXdzWnJf94Wo1DcnuGkfijbf3AuBhfs` (verified against mainnet-beta). FE imports (`useGateway`, `GatewayStatus.ACTIVE`, `GatewayProvider`, `findGatewayToken`) verified against installed types. New `Anchor.toml` `[[test.validator.clone]]` entry cloning Civic from mainnet-beta on `anchor test` start. Runtime `tests/civic-happy-path.spec.ts`: `test_civic_gate_allows_with_valid_token` + `test_civic_gate_rejects_after_revoke` — uses `@identity.com/solana-gateway-ts` to `addGatekeeper` → `issue` → assert byte layout → `revoke` → assert state byte flip (0 Active → 2 Revoked). **35/35 anchor tests green** (33 baseline + 2 new). `scripts/dev/init-civic-configs.ts` — idempotent Devnet helper; `pnpm init:civic --custodian <pubkey>` initializes vault_config + loan_config with the CAPTCHA network `ignRE...`; exit 2 SKIPPED on missing prerequisites. `README.md` gains "Civic Pass KYC" section with demo-model (CAPTCHA) + production-upgrade path (full KYC via paid Civic gatekeeper). USER_TODO §5 migrated to "resolved" status with remaining user action to enable the gate live. Commit `67e7120`.

## 2026-04-24 — Editorial dark-operator design pass

- **Design system overhaul (commit `a0e0ba5`):** Archetype committed as Editorial Magazine × Dark Operator — "a luxury financial newspaper operating in a private members' club at 2am." Differentiator: live on-chain ticker of real Supabase `onchain_events` (with 12-event seeded fallback). **Tokens:** Fraunces (variable serif, opsz 9–144) display + Instrument Sans body + JetBrains Mono numerals; ink-black `#0A0B0D`/paper-warm `#F4F2ED`/restrained gold `#D4AF37`/hairline `#22262D`; motion `cubic-bezier(0.22,1,0.36,1)` with 60ms stagger; 3.5% SVG grain overlay fixed over everything. Dark default, light toggleable, no-FOUC boot script. **10 new shared components** (`site-header`, `site-footer`, `onchain-ticker`, `grain-overlay`, `metric-card`, `pullquote`, `step-rail`, `editorial-section`, `marquee-rule`, `theme-toggle`) + new `/api/onchain-events/ticker` route (service-role Supabase OR seeded fallback, exposes `source: 'live' | 'seeded'`). **Landing `/` fully rebuilt**: left-aligned hero with italic-gold accent word, asymmetric 3-column "How Vaulx works", 4-up live metrics, edge-to-edge Portuguese pullquote, 9-moments rail with snap-x, stylized CCB document cutaway w/ blurred gold hash ribbon, closing CTA. **Lender dashboards** (`/lend`, `/lend/vaults`, `/lend/vaults/[id]`) rebuilt as operator tables with hairline rows + gold-on-hover left-rules + 7/5 deposit-form split. **Borrower wizard** gains persistent `<StepRail>` (5 steps, pathname-resolved) + editorial-section wrappers + asymmetric 5/7 split on terms. **Custodian intake** restyled as museum-registrar journal with numbered check-register (ticked items get gold underline) + oversized mono doc-hash input. Gov.br pages intentionally left untouched (government-site mimic is the joke). Shadcn atoms kept intact via HSL-triplet derivation from hex tokens. Web build + typecheck + vitest all green.

## 2026-04-24 — Phase 2 complete (tasks 2.1–2.11)

- **Task 2.11 completed (Phase 2 close-out):** Tagged `phase-2-done`. All 12 Phase 2 tasks landed: on-chain custody + CPI-only disburse gate (33 anchor tests green), BRD §7 test coverage, IDL freeze, real CCB generator + hasher, I1 appraisal aggregator API, **real Civic Pass integration** (on-chain feature-flag gate + FE SDK swap, replaces Phase 1 mock), I2 gov.br mocked ID flow, borrower wizard (Moment 2), awaiting-custody + custodian intake (Moment 3), Moments 2+3+4 E2E harness. All web builds + typecheck + vitest green. Open: 6 `TODO(civic-sdk-verify)` markers for user to close post-SDK-install. Next: Phase 3 (repayment, renewal, auction, Solana Pay, SSE test runner).
- **Task 2.10 completed (Moments 2+3+4 E2E harness):** `scripts/dev/moments-2-3-4-e2e.ts` (460 lines) mirrors `moment-1-e2e.ts` pattern: prechecks (Supabase env, seeds, payer SOL, vault liquidity ≥ 200 USDC) → exit 2 SKIPPED on any miss → `ensureLoanConfig(custodian, Pubkey::default())` + `ensureVaultConfig` → airdrop borrower/custodian if < 0.01 SOL → **Moment 2** `createCcbTrdc(100e6 appraisal, 60e6 loan, 30d term, rand32 asset_hint)` + poll `ccbTrdcCreated` + assert loan_id/amount → **Moment 3** `confirmCustody(rand32 doc_hash)` signed by custodian + poll `custodyConfirmed` + assert hex doc_hash match + fetch TRDCState=ActiveInCustody → **Moment 4** `disburseFromVault(60e6)` signed by borrower with 12 accounts (incl. `loan_authority` PDA + `SYSVAR_INSTRUCTIONS_PUBKEY`) + poll `disbursed` (vault-emitted in the CPI, lowercased per Anchor 0.30.1) + assert borrower ATA delta + vault ATA delta + vault.total_assets delta + TRDCState=Active. Mocha wrapper `tests/moments-2-3-4-e2e.spec.ts` (180s timeout, exit-2→skip). Root `pnpm e2e:moments-2-3-4`. Uses raw Anchor `Program` from IDL JSON (not `@vaulx/anchor-client`) to decouple from workspace build. typecheck green. Commit `0cd9196`.


- **Task 2.9 completed (Moment 3 — awaiting-custody + custodian intake):** Borrower awaiting-custody page replaces 2.8 placeholder: reads sessionStorage breadcrumb `vaulx_loan_${trdc}`, renders shipping card (hardcoded Vaulx Custody address) + loan details + status spinner, polls `/api/onchain-events/custody-confirmed` every 3s via TanStack Query + auto-advances to success state + "Continue to disbursement" link to placeholder `/borrow/loans/[trdc]/disburse`. Dev-only deeplink to custodian intake when `NEXT_PUBLIC_VAULX_DEV_SHORTCUTS="1"`. Custodian intake `/custodian/intake/[trdc]` gates: wallet connected → matches `LoanConfig.custodian` → TRDCState exists → status == `PendingCustody`; shows read-only TRDC details, 5-item intake checklist (all required), 64-hex `doc_hash` field prefilled from `?hash=`, SOL balance warning < 0.001, clear "LoanConfig not initialized" banner. Confirm → `loan.confirm_custody(doc_hash)` → toast + redirect to placeholder `/custodian/done/[trdc]`. New `lib/chain/custody.ts::{useLoanConfig, useTrdcState, useConfirmCustody}`. **Indexer extended to subscribe to both vault + loan programs** (shared Subscription list; events land in `onchain_events` with `program_id` distinguishing). API route `/api/onchain-events/custody-confirmed` uses Supabase service role directly (not `@vaulx/supabase/server` — its zod validator conflicts with graceful fallback), queries lowercased `"custodyConfirmed"` defensively against both `trdc_state` + `trdcState` payload keys, decodes `doc_hash` array-or-string to hex. Web + indexer typecheck + build green. Commit `29afbb4`.


- **Task 2.8 completed (borrower wizard — Moment 2):** 3 wizard pages + 4th placeholder for Task 2.9. `/borrow/new/asset`: RHF + Zod watch form (make preset + other, 1950→now year bound, condition radio), POSTs `/api/appraisal`, stashes response under sessionStorage key `vaulx_appraisal_${reqId}`. `/borrow/new/appraisal/[reqId]`: 3 source cards (Chrono24/WatchCharts/Vaulx Model) with live/fallback/error badges + median card + Continue locks median into `_locked` subkey. `/borrow/new/terms/[reqId]`: LTV range slider 10–60%, 30/60/90d radio, rate table 8/10/12% APR, due-date display, CCB document-card preview rendering the full SHA-256 hash + Download button, Confirm flow — generates fresh `loan_id` keypair, derives TRDCState PDA, regenerates CCB with final loanId, best-effort uploads to Supabase `ccb-pdfs/` bucket, calls `create_ccb_trdc(loan_id, appraisal_atoms, loan_atoms, dueTs, assetHint)` where `asset_hint` = first 32 bytes of CCB hash, redirects to `/borrow/new/awaiting-custody/${trdcPda}`. New shared components `<GovbrGate>` (mirrors Civic pattern, preserves `return_to`) + `<IdentityGates>` (stepped Civic→gov.br; gracefully skips Civic if env unset). New chain clients `lib/chain/loan.ts::{deriveTrdcStatePda, deriveLoanConfigPda, generateLoanId, useCreateCcbTrdc}` + `lib/chain/ccb-storage.ts` (best-effort Supabase upload). Web build green (asset static, dynamic params pages as expected), `pnpm --filter @vaulx/web test` 11/11 green, typecheck 8/8. sessionStorage breadcrumb `vaulx_loan_${trdcPda}` left for Task 2.9. Commit `635e0e2`.


- **Task 2.7 completed (I2 gov.br mocked ID):** 4 Next.js App Router pages at `/borrow/verify-id/{,redirecting,govbr-login,callback}` styled with gov.br colors (`#1351B4` blue header, system-ui font) + persistent "Demo mode — not a real government service" badge. Entry page → 1s "Redirecting to gov.br.gov…" spinner → fake login (CPF masked input `000.000.000-00`, any password, real check-digit validation via `validateCpf` — rejects `111.111.111-11` reserved pattern + malformed check digits) → 2s "Autenticando…" spinner → writes `vaulx_govbr_<wallet>` localStorage with `{ cpf, name, verified_at }` (name deterministically derived from 10-pool via hash-index) → callback page shows masked CPF + "Return to loan application" button. `?mock=auto` shortcut completes end-to-end in ~2s and preserves `return_to` across redirects. Hand-rolled CPF input mask (no `react-imask` dep). `useGovbrVerification(wallet)` hook exported for Task 2.8 wizard consumption. Each page wraps in `<Suspense>` (Next 14 static-prerender requirement with `useSearchParams`). 5/5 cpf.test.ts vitest green; web build clean with all 4 routes prerendered. Commit `96e54b3`.


- **Task 2.6.5 completed (REAL Civic Pass integration):** Promoted I4 from the Phase 1 hardcoded mock to on-chain + FE real integration. **On-chain:** new `VaultConfig` PDA at `[b"vault_config"]` (fields `admin, civic_network, bump`) with `initialize_vault_config(civic_network)`; `LoanConfig` extended with `civic_network` (breaking sig change to `initialize_loan_config(custodian, civic_network)`). `programs/vault/src/civic.rs` + `programs/loan/src/civic.rs` implement manual gateway-token verification (owner = Civic gateway program, Borsh layout parse, state byte = Active). `vault.deposit` + `loan.create_ccb_trdc` check the token only when the config's `civic_network != Pubkey::default()` → **feature-flag gate**, keeps existing 29 tests green. 4 new civic-gate smoke tests in `tests/civic-gate.spec.ts` assert IDL wiring + error-variant presence. Total **33 anchor tests passing**. **FE:** installed `@civic/solana-gateway-react` + `@identity.com/solana-gateway-ts`; `<CivicPassGate>` wraps lender deposit form; `<GatewayProvider>` conditionally wraps wallet tree when `NEXT_PUBLIC_CIVIC_PASS_NETWORK` is set. Deleted `kyc-mock-modal.tsx` + all `vaulx_kyc_<wallet>` localStorage code. `apps/web/.env.example` documents the network env. Moment-1 E2E harness updated to init `vault_config` (gate-off) and pass the new accounts. `pnpm --filter @vaulx/web build` + `pnpm -w typecheck` green. **6 `TODO(civic-sdk-verify)` markers** remain inline for user to close after verifying installed SDK exports (`useGateway`/`GatewayStatus` enum shape, `findGatewayToken` signature, `<GatewayProvider>` prop shape, gateway token PDA seed nonce byte, state-byte offset). **Known issue:** the hardcoded `CIVIC_GATEWAY_PROGRAM` pubkey `gatem74V238NmbRnHDf4XHJyqjx6YF3GHJqjUw1GJU` decoded to 31 bytes so the subagent appended `9` to pad to 32 — verify this matches the installed SDK's canonical const before using on Devnet. Commit `bc7ce5c`.


- **Task 2.6 completed (I1 appraisal aggregator):** `POST /api/appraisal` (Next.js App Router, `runtime="nodejs"`, `dynamic="force-dynamic"`, `Cache-Control: private, max-age=60`). Zod-validated `AppraisalInput`. Three sources fanned out via `Promise.all` + per-source `withTimeout` (10s): Chrono24 (direct fetch + `node-html-parser`, 3 extraction strategies with regex fallback; never returns `ok:false` — always emits `fallback:true` stub on any failure), WatchCharts (real API behind `WATCHCHARTS_API_KEY` env; else 20-ref JSON fixture at `fixtures/watchcharts-fallback.json` with Rolex/Patek/AP/Omega/IWC + nearest-match by make/model, default $10k), internal (deterministic TS: `base × (1 − ageDiscount + youthPremium) × conditionMult`; Rolex 116610LN/2020/excellent → $14,500). Median computed over `ok` values; returns 502 if all sources dead. 6/6 vitest green (happy path, chrono24 timeout, unknown ref, ±15% snapshot, condition + age sensitivity). Added `apps/web/vitest.config.ts` + `node-html-parser`. Web build + typecheck clean; dynamic route `/api/appraisal` registered. Commit `877977c`.


- **Task 2.5 completed (`@vaulx/ccb` real generator):** Replaced the `TODO = true` stub with a real CCB.B3 PDF writer. `generateCcbPdf(input: CcbInput)` lays out a one-page A4 document from scratch via `pdf-lib` (header + parties + garantia + condições financeiras + 4 Portuguese cláusulas + 3 signature lines + footer). Accents stripped for Helvetica WinAnsi safety. Metadata pinned to `issuedAtTs` + `Producer`/`Creator` hardcoded to `vaulx-ccb` + `useObjectStreams: false` on save → byte-identical output across runs. PDF `Keywords` carries raw atoms + rate bps + term days so the SHA-256 is sensitive to sub-cent changes the 2-decimal render would hide. `hashCcb(bytes)` returns `{ digest, hex }` via `@noble/hashes/sha256`. 4/4 vitest green: non-empty Uint8Array starting `%PDF-`, 32-byte digest + 64-char hex, byte-equal determinism across two identical calls, 1-atom delta flips the hash. `pnpm -w typecheck` + `pnpm -w test` green. Commit `1917680`.


- **Task 2.4 completed (IDL freeze):** Tagged `phase-2-idl-freeze` at commit `b4cd6cb`. Re-ran `node scripts/dev/gen-clients.mjs` against the frozen Anchor 0.30 IDLs — `anchor-client-gen@latest` still errors with `Unreachable.` on `trdc.json` (same root cause as Phase 1 Task 1.10: top-level `address` + `metadata` + inline `discriminator` arrays not yet parsed upstream). Hand-rolled façade in `packages/anchor-client/src/index.ts` remains canonical; `packages/anchor-client/src/generated/.gitkeep` reserves the slot for when upstream adds Anchor 0.30 support. `pnpm -w typecheck` green (7/7 cached).


- **Task 2.3 completed (BRD §7 named tests):** 8 new first-class `it()` blocks promoted from informal coverage — `test_ltv_exactly_at_limit_accepted` (60.00%), `test_deposit_rejects_zero_amount`, `test_withdraw_rejects_over_balance` (liquidity guard fires before SPL burn CPI), `test_ccb_create_requires_nonzero_amount`, `test_trdc_state_transition_rejects_illegal` (PendingCustody→Liquidated reverts `InvalidStateTransition`), `test_mint_trdc_cnft_writes_stable_asset_id` (asserts on-chain `asset_id` == TS `SHA-256(loan_id || asset_hint)` + stable across repeat calls), `test_deposited_event_fields_match` (parses tx logs via `EventParser` + `BorshCoder`, field-by-field match incl. lowercased `deposited`), `test_vault_pda_derivation_is_deterministic` (2× `findProgramAddressSync` identical; post-init owner = vault program). 29/29 anchor tests green; no program code changes. Commit `f113582`.


- **Task 2.2 completed (CPI-only disburse gate):** `vault.disburse` replaced with a real two-layer gate: **Layer 1** — `loan_authority` account (PDA `[b"loan_authority"]` in loan program) must equal the expected derivation and be a signer; **Layer 2** — `instructions` sysvar inspection asserts the top-level transaction's programId == loan program (not a direct vault call). Transfers USDC from `vault_ata` → `borrower_ata` with the vault PDA signing, decrements `total_assets` (shares NOT burned — this is a loan, not a withdrawal). Emits `Disbursed { vault, borrower, amount, total_assets, ts }`. Loan program adds `disburse_from_vault(amount)` which requires `TRDCState.status == ActiveInCustody`, CPIs via `invoke_signed` with `[b"loan_authority"]` seeds into `vault.disburse`, then CPIs into new `trdc.transition_to_active` (ActiveInCustody → Active), emits `DisburseRequested`. New tests in `tests/disburse.spec.ts`: `test_disburse_fails_when_custody_not_confirmed` (→ `InvalidStateTransition`), `test_disburse_fails_with_unauthorized_caller` (→ `UnauthorizedDisbursar`), `test_disburse_happy_path` (50k of 100k liquidity disbursed, state → Active). Hardcoded `LOAN_PROGRAM_ID` in vault as `Pubkey::new_from_array([...])` to avoid circular crate dep (vault ↛ loan). Shared custodian helper in `tests/_shared.ts` makes `loan_config` init idempotent across spec files. Removed the `DisburseNotYetImplemented` assertion test (obsolete). 21/21 anchor tests green; typecheck green. Commit `097b3a8`.


- **Task 2.1 completed (confirm_custody + ActiveInCustody + doc_hash):** Added intermediate `ActiveInCustody` state between `PendingCustody` and `Active`; transition table now `PendingCustody→ActiveInCustody→Active` (11 legal edges). New `doc_hash: [u8;32]` field on TRDCState (reserved shrunk 64→32 for zero net layout change). New `LoanConfig` PDA at `[b"loan_config"]` (fields `admin`, `custodian`, `bump`) initialized by `loan.initialize_loan_config(custodian)`. New `loan.confirm_custody(doc_hash)` ix: validates signer == loan_config.custodian (else `UnauthorizedCustodian`), CPIs into new `trdc.confirm_custody_transition(doc_hash)` which writes hash + transitions state, emits `CustodyConfirmed`. Tests in `tests/custody.spec.ts`: happy path + unauthorized signer revert. `tests/trdc.spec.ts` transition-table tests updated to walk through ActiveInCustody. 19/19 anchor tests green; typecheck clean; IDLs regenerated via `copy-idls.sh`. Tight CPI-only gate (caller must be loan program) intentionally deferred to Task 2.2 as `PHASE_2_TASK_2_2_TODO`. Commit `1cd3355`.

## 2026-04-24 — Phase 1 complete (tasks 1.1–1.14)

- **Task 1.14 completed (Moment 1 E2E harness):** `scripts/dev/moment-1-e2e.ts` + `tests/moment-1-e2e.spec.ts` exercise the full Devnet round-trip — derive vault PDA, init-if-missing, ensure ATAs, deposit 100 USDC from demo wallet 0, poll Supabase `onchain_events` (1s × 30s) for a `deposited` row matching the tx signature, and assert `payload.amount === "100000000"` + `shares_minted === "100000000"` (1:1 strict only on runs that initialized the vault; otherwise non-zero). Exit codes 0/2/1 = pass/SKIPPED/fail; the Mocha wrapper (`spawnSync npx tsx …`) maps 2 → `this.skip()` so CI stays green until env is set. Preconditions checked up front: `SUPABASE_URL|NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` env (loaded from `apps/indexer/.env.local` + `apps/web/.env.local`), seed JSONs present, payer ≥ 0.05 SOL. Off-curve (PDA) ATA creation uses a raw `createAssociatedTokenAccountInstruction` tx rather than `getOrCreateAssociatedTokenAccount` (spl-token helper has edge cases with off-curve owners). Event assertion uses lowercase `"deposited"` per Anchor 0.30.1 gotcha. Added root devDeps `dotenv` + `@supabase/supabase-js`; root script `pnpm e2e:moment-1`. typecheck 7/7 green; standalone run exits 2 (SKIPPED) as expected. Commit `a8d2a72`. **Phase 1 closed.**
- **Task 1.13 completed (indexer worker + `onchain_events`):** `apps/indexer` (tsx/Node ESM, NodeNext resolution) subscribes to the vault program via `connection.onLogs(programId, ..., "confirmed")`, parses logs with Anchor `EventParser` + `BorshCoder`, and upserts into `public.onchain_events` on Supabase. Migration `supabase/migrations/20260425000000_onchain_events.sql` applied via Supabase MCP (creates `onchain_events` with `unique(signature)` + indexes on `(program, slot)` and `(event_name)`). Gracefully ignores Postgres 23505 unique-violation on replay. Documents the Anchor 0.30.1 lowercase-event-name gotcha inline. Live verification blocked on `SUPABASE_SERVICE_ROLE_KEY` (dashboard-only). typecheck 7/7 green. Commit `386567b`.
- **Task 1.12 completed (lender FE /lend flow + I4 Civic/Blockpass mock):** 3 Next.js 14 App Router pages (`/lend`, `/lend/vaults`, `/lend/vaults/[id]`) + deposit form (React Hook Form + Zod, ≥ 1 USDC) + KYC mock modal (3s fake Civic/Blockpass verify, localStorage-keyed per wallet `vaulx_kyc_<wallet>`). TanStack Query for vault state + user balances; sonner toasts. `lib/chain/vault.ts` exposes `deriveVaultPda`, `useVaultData`, `useUserShareBalance`, `useUserUsdcBalance`, `useDeposit` (wires all 8 accounts into the Anchor builder). `/lend` → 200 w/ "Browse vaults"; `/lend/vaults` → 200 w/ "USDC" (or empty state until mint seeded). Web build + lint + typecheck green. Commit `529fd9f`.
- **Task 1.11 completed (seed script):** `scripts/dev/seed-usdc.ts` — idempotent Devnet USDC mint + 6-wallet seeder (2 SOL + 50k USDC each). Re-reads `scripts/dev/devnet-usdc.json` + `scripts/dev/demo-wallets.json` on each run to only fund the delta. Payer underfunded-guard path verified (payer `2HYjytRc4oKY2ndmJfAq2XdGhPqYB7VdDPLzA18QEiAH` has 0 SOL — CLI airdrop rate-limited; user must fund at faucet.solana.com before the mint materializes). Added `tsx` + `@solana/web3.js` to root dev-deps (web3.js was transitive-only via spl-token and didn't hoist). Root `pnpm seed:usdc` script wired. typecheck 6/6 green. `.gitignore`s exclude `demo-wallets.json` + `devnet-usdc.json`. Commit `a124674`.

## 2026-04-24 — Phase 1 planning + tasks 1.1–1.10

- **Task 1.10 completed (anchor-client):** anchor-client-gen 0.28.1 (latest published) errors on Anchor 0.30 IDL shape (`address` top-level, `metadata`, inline `discriminator` arrays). Switched to the plan-authorized fallback: hand-rolled façade at `packages/anchor-client/src/index.ts` that re-exports the raw IDLs from `@vaulx/idls` and exposes `{ idl, programId, program(provider) }` per program via `new Program<Idl>(idl, provider)`. `build:client` script + `scripts/dev/gen-clients.mjs` stay wired so we can flip to real generated clients when upstream catches up. Added `@coral-xyz/anchor ^0.30.1` + `@vaulx/idls workspace:*` deps. `pnpm -w typecheck` → 6/6 green. Commit `e4c64be`.

## 2026-04-24 — Phase 1 planning + tasks 1.1–1.9

- **Task 1.9 completed (events + IDL copy):** added 6 `#[event]`s across trdc/vault/loan — trdc `TrdcStateInitialized` + `TrdcTransitioned`; vault `VaultInitialized` + `Deposited` + `Withdrawn`; loan `CcbTrdcCreated`. Emits placed after all state mutation / CPIs so captured totals reflect post-state. `scripts/dev/copy-idls.sh` (executable, `set -euo pipefail`) copies all 4 IDLs from `target/idl/*.json` → `packages/idls/src/*.json`. `packages/idls/src/index.ts` re-exports all 4; `tsconfig.base.json` already had `resolveJsonModule: true`. Added listener-based `deposit emits a Deposited event` test with `getTransaction().meta.logMessages` fallback parse to avoid localnet finalized-commitment WS flakiness. 17/17 Anchor tests green; `pnpm -w typecheck` green (5/5 packages). **Indexer gotcha:** Anchor 0.30.1 runtime lowercases the first letter of event names for `addEventListener` — Task 1.13 must subscribe to `"deposited"` / `"withdrawn"` / `"ccbTrdcCreated"` etc., not the PascalCase struct name. Commit `798b208`.

## 2026-04-24 — Phase 1 planning + tasks 1.1–1.8 (both exit-criteria tests green)

- **Task 1.8 completed (Loan.create_ccb_trdc with LTV gate + CPI into TRDC):** new `create_ccb_trdc(loan_id, appraisal_value, loan_amount, due_ts, asset_hint)` instruction in loan program. Enforces `loan_amount * 10_000 <= appraisal_value * 6_000` (≤60% LTV, mirroring `packages/terms/src/ltv.ts`) via u128 checked_mul. CPIs into `trdc::initialize_trdc_state` + `trdc::mint_trdc_cnft`. New `programs/loan/src/errors.rs` with `MAX_LTV_BPS = 6_000` + `LoanError {LtvTooHigh, ZeroAmount, MathOverflow}`. Added `trdc = { path = "../trdc", features = ["cpi"] }` to loan's Cargo.toml. Tests: `test_ltv_enforced_at_mint` (61% → `LtvTooHigh`) + happy-path (59% → TRDCState in PendingCustody with non-default asset_id). 16/16 Anchor tests passing. Commit `3e8324e`.
- **Both Phase 1 exit-criteria tests now green:** `test_vault_share_accounting` (vault share math invariant, task 1.5) + `test_ltv_enforced_at_mint` (LTV gate, task 1.8).

## 2026-04-24 — Phase 1 planning + tasks 1.1–1.7

- **Phase 1 detailed plan written:** `docs/plans/2026-04-25-vaulx-phase-1-core-programs.md` (14 tasks, TDD-granular, covers TRDC 7-state PDA, Vault share accounting, Loan LTV gate, lender FE, indexer worker; Bubblegum + Civic-on-chain gate deferred to Phase 2).
- **Tasks 1.1–1.3 completed (TRDC program):** `TRDCState` PDA + 7-state Status enum (`PendingCustody → Active → {Renewed|Repaid|Overdue} → Defaulted → Liquidated`), transition table enforced on-chain via `InvalidStateTransition`, `mint_trdc_cnft` stub writes a deterministic hash-based `asset_id` (real Bubblegum CPI deferred to Phase 2).
- **Task 1.4 completed (Vault init):** `initialize_vault` creates `Vault` PDA (seeds `[b"vault", asset_mint]`), USDC reserve ATA, share mint owned by the vault PDA. `anchor-spl` wired with `token` + `idl-build` features.
- **Task 1.5 completed (deposit):** `deposit(amount)` mints shares using u128-intermediate checked math (`shares_out = floor(amount * total_shares / total_assets)`). Required invariant test `test_vault_share_accounting` green (first deposit 1:1, second rounds down, multi-depositor proportional). Added test-only `test_donate_assets` helper to let rounding tests bump `total_assets` without minting.
- **Task 1.6 completed (withdraw):** `withdraw(shares)` burns caller's shares + transfers `floor(shares * total_assets / total_shares)` back from the vault reserve with vault PDA signer seeds. Tests: deposit+withdraw roundtrip within ±1 lamport, over-withdraw reverts. Code review flagged dust-withdraw (shares round to zero assets) → added `require!(assets_out > 0, ZeroAmount)` guard (commit `4aea6a0`). All 13 Anchor tests passing.
- **Task 1.7 completed (disburse stub):** `disburse(_ctx, _amount)` instruction signature + full `Disburse` accounts struct published; body returns `err!(VaultError::DisburseNotYetImplemented)`. Lets anchor-client-gen emit the typed builder now so the FE can wire a disabled "coming in Phase 2" button without blocking on backend work. Custody-gated CPI body lands in Phase 2. Test asserts the exact error code name. 14/14 Anchor tests passing.

---

## 2026-04-23 — Phase 0 kickoff

- Wrote design doc: `docs/plans/2026-04-23-vaulx-full-stack-build-design.md`
- Wrote implementation plan: `docs/plans/2026-04-23-vaulx-build-plan.md`
- Started Phase 0 bootstrap execution (subagent-driven).
- Kicked off Rust 1.79 + Solana CLI 1.18.26 installation in background.
- **Task 0.1 completed:** initialized git repo on `main`, added `.gitignore` (incl. `.claude/`) and minimal `README.md`, committed design docs + STATUS + CHANGELOG. Legacy root-level `*.docx`/`*.pdf`/`*.png`/pre-Vaulx planning `.md` files left untracked pending triage.
- **Task 0.2 completed:** initialized pnpm workspace + Turborepo at repo root (`package.json`, `pnpm-workspace.yaml`, `turbo.json`, `pnpm-lock.yaml`); `packageManager` pinned to `pnpm@10.13.1` to match local install; `pnpm -w exec turbo --version` reports `2.9.6`.
- **Task 0.4 in progress:** Anchor workspace scaffolded (4 program crates `trdc`/`vault`/`loan`/`auction` with `ping`, workspace `Cargo.toml`, `Anchor.toml`, `rust-toolchain.toml`, `tests/bootstrap.ts`, `@coral-xyz/anchor@0.30.1`). `anchor keys sync` generated real pubkeys. `anchor build --no-idl` succeeds. Full `anchor build` blocked: anchor-syn 0.30.1 calls removed `proc_macro2::Span::source_file()` (API removed in pm2 1.0.106+). Unblock path: bump Rust toolchain to 1.85+, revert transitive-dep `cargo update --precise` downgrades, rerun `anchor build`.
- **Task 0.4 completed:** Anchor workspace green. rustc upgraded to 1.85.0 to unblock proc-macro2 API requirements. `anchor build` + `anchor test` pass; all 4 programs deploy and respond to `ping`. Real program IDs committed. Final blocker was resolved by vendoring `anchor-syn-0.30.1` at `vendor/anchor-syn` (via `[patch.crates-io]`) with the unused `source_file()`-dependent cross-file type alias path disabled — the API was removed from stdlib post-1.79, and the codepath it gated is not needed for our programs. Transitive crates that require `edition2024` (block-buffer 0.12, borsh 1.6, hashbrown 0.17, indexmap 2.14, proc-macro-crate 3.5, unicode-segmentation 1.13) remain pinned to pre-edition2024 releases because platform-tools 1.41 ships cargo 1.75. `solana-test-validator` on macOS required `COPYFILE_DISABLE=1` to avoid `._genesis.bin` resource-fork in the genesis tarball.
- **Task 0.3 completed:** scaffolded shared packages `@vaulx/types`, `@vaulx/terms`, `@vaulx/ccb`, `@vaulx/anchor-client`, `@vaulx/idls`; added root `tsconfig.base.json`; TDD for LTV math in `@vaulx/terms` (test-first red, then impl green; `pnpm -r test` → 4/4 passing on vitest 1.6.x).
- **Task 0.5 completed:** scaffolded `apps/web` (`@vaulx/web`) as Next.js 14 App Router with Tailwind (Vaulx palette `brand.blue` / `brand.gold`, Inter + Manrope via `next/font`), shadcn/ui `new-york` + neutral base with primary overridden to Vaulx deep blue (button component), Phantom + Solflare wallet adapter wired via `WalletProvider` on Devnet (RPC from `NEXT_PUBLIC_RPC_URL`); `@coral-xyz/anchor` pinned to 0.30.1; turbo `build`, `lint`, `typecheck` all green; dev server smoke test returned 200 with "Vaulx" in body.
- **Task 0.6 completed:** added `.github/workflows/ci.yml` with two parallel jobs on push/PR to `main` — `ts` (pnpm install + turbo lint/typecheck/test + web build on Ubuntu 22.04 / Node 20) and `anchor` (rustc 1.85.0 via dtolnay/rust-toolchain, Solana 1.18.26 via Anza installer, Anchor CLI 0.30.1 via `cargo install --locked`, throwaway keypair, `anchor build` + `anchor test`); caches: `Swatinem/rust-cache` keyed on `Cargo.lock`, Solana install keyed on version, Anchor CLI keyed on `anchor-cli-0.30.1`; `concurrency: cancel-in-progress` per ref. No package-level script changes needed — turbo 2.9.6 tolerates packages without a given task script.
- **Task 0.7 in-repo scaffold:** @vaulx/supabase package with env validation, server/browser clients, placeholder Database types; /api/health route wired; root + app .env.example covering Supabase + Helius. Awaiting user keys.
- **Task 0.9 completed:** Phase 0 exit verification passed — all TS/Anchor/repo/health checks green.
- **Phase 0 fully closed.** Private GitHub remote [gogysss/vaulx](https://github.com/gogysss/vaulx) created via `gh repo create`; `main` pushed. Supabase project `vaulx-devnet` (free tier, `us-east-1`, id `ctiypfxtymnszposgaky`) created via Supabase MCP; URL + anon + publishable keys wired into `apps/web/.env.local` (gitignored). `/api/health` now reports `supabase: configured, rpc: configured`. Outstanding: `SUPABASE_SERVICE_ROLE_KEY` (dashboard-only, needed for Phase 2 admin writes), `HELIUS_API_KEY` (optional — `api.devnet.solana.com` OK for Phase 1). Design decisions D1–D7 set to plan defaults per user ("proceed with defaults").
